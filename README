 	 A splunk application to handle logs sent from syslog-ng to a central splunk server.

FEATURES
* Handles ISODATE as specified by syslog-ng 2.
* Adds a new input for tcp:9998
* Detects hostname
* Extracts reporting facility and priority
* Extracts process name and pid
* Sets sourcetype per event. Currently detects rails, postfix, access_combined_wcookie. Defaults to syslog.

TODO
